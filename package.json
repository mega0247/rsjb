{
  "name": "redirect-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for handling redirects",
  "main": "src/worker.js",
  "scripts": {
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
